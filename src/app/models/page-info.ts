export interface PageInfo {
  pageNumber: number;
  /** Rendered pixel width at scale=1 */
  width: number;
  /** Rendered pixel height at scale=1 */
  height: number;
  rotation: number;
  scale: number;
}
